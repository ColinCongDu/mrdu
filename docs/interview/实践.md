### 虚拟滚动怎么做

虚拟滚动的核心目标是：在大数据量场景下，通过只渲染可视区域的数据，降低DOM数量，从而提升渲染性能。

虚拟滚动的实现本质是：
1. 维护一个可视区域（viewport）
2. 根据滚动位置计算当前应该渲染的数据范围（startIndex / endIndex）
3. 只渲染这一段数据
4. 通过占位元素（padding 或 transform）撑起整体高度，让滚动条正常工作


