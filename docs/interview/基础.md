# 基础知识

## JS 宏任务和微任务

任务的分类：

1. 宏任务：script、setTimeout、setInterval、setImmediate、I/O、UI 渲染
2. 微任务：process.nextTick、Promise.then、Object.observe、MutationObserver

事件循环核心规则:

- "同步先跑 → 微任务全清空 → 才跑下一个宏任务"
- 微任务会一次性全部执行完，中间不会插入宏任务
- 宏任务每次只执行一个，执行完必须等微任务全部清空，才会执行下一个宏任务
